int main [float x] {
    int y;
    bool b;
    y := b + 2 / (x -1);
}

int f {
    bool bubuzinha;
    return bubuzinha;
}

if x < 0 then 
    x := 2; 
else {
    int x
    y := 2;
}

while x > 0 do {
    int x;
}

y := 2;

bool b;
b := true;
